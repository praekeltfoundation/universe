{
  "type": "object",
  "properties": {
    "marathon-acme": {
      "description": "marathon-acme service properties",
      "type": "object",
      "properties": {
        "name": {
          "description": "The name of this DC/OS service.",
          "type": "string",
          "default": "marathon-acme"
        },
        "cpus": {
          "description": "CPU shares to allocate to marathon-acme.",
          "type": "number",
          "default": 0.1,
          "minimum": 0.01
        },
        "mem": {
          "description": "Memory to allocate to marathon-acme.",
          "type": "number",
          "default": 128.0,
          "minimum": 128.0
        },
        "instances": {
          "description": "The number of marathon-acme instances to start.",
          "type": "integer",
          "default": 1,
          "minimum": 1,
          "maximum": 1
        },
        "role": {
          "default": "*",
          "description": "Deploy marathon-acme only on nodes with this role.",
          "type": "string"
        },
        "allow-multiple-host-instances": {
          "description": "Whether it should be allowed that multiple instances of the marathon-acme can exist on the same agent.",
          "type": "boolean",
          "default": false
        },
        "haproxy-group": {
          "default": "external",
          "description": "The marathon-lb group to issue certificates for.",
          "type": "string"
        },
        "marathon-uri": {
          "default": "http://marathon.mesos:8080",
          "description": "The addresses for the Marathon HTTP API.",
          "type": "string"
        },
        "lb-uri": {
          "default": "http://marathon-lb.marathon.mesos:9090",
          "description": "The addresses for the marathon-lb HTTP API.",
          "type": "string"
        },
        "acme-email": {
          "description": "An email address to register with the ACME service.",
          "type": "string"
        },
        "acme-uri": {
          "description": "The address for the ACME Directory Resource.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "cpus",
        "mem",
        "haproxy-group",
        "marathon-uri",
        "lb-uri"
      ]
    }
  },
  "required": [
    "marathon-acme"
  ]
}
